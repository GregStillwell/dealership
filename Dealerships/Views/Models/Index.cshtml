@{
  Layout = "_Layout";
}

@using Dealership.Models;
@model List<Dealership.Models.Makes>;

<h1>Car Makes</h1>

@if (@Model.Count == 0)
{
  <h3>No car makers have been added yet!</h3>
}
else
{
  <ul>
  @foreach (CarModels carmodel in Model)
    {
      <li>@Html.ActionLink($"{model.Name}", "Details", new { id = model.CarModelId }) | @model.Makes.Name</li>
    }
  </ul>
}

<p>@Html.ActionLink("Add new model", "Create")</p>